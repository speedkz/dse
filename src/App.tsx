import { Provider } from "@/components/ui/provider";
import { Button } from "./components/ui/button";

function App() {
  return (
    <Provider>
      <Button>test</Button>
    </Provider>
  );
}

export default App;
